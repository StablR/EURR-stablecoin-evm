# This is a basic workflow to help you get started with Actions

name: CI

on: 
  push:
    branches:
      - "*"
jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: Ganache
        run: yarn ganache
      - name: Test
        run: yarn test
  
